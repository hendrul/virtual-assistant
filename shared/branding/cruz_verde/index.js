const avatar = require('./avatar.png')
const theme = require('./theme')
const strings = require('./strings')
require("./reset.css")

window.appStrings = strings
module.exports = { avatar, theme, strings }